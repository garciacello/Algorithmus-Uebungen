#include <stdio.h>
float berechnenQuadrat (float Laenge, float Breit)
{
	float flaechen = Laenge * Breit;


return flaechen;
}

int main (void){

	float berechnenQuadrat (float x, float y);
	float flaechen = berechnenQuadrat( 15.0, 50.0);
	printf("A area é:%f", flaechen);

return 0;
}


